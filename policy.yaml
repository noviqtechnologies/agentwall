# AgentWall Policy — auto-generated by `agentwall init --from-log`
#
# This policy was generated from a dry-run session log.
# All observed tools are pre-populated with action: allow.
# Review each tool and its parameters carefully before enabling enforcement.
#
# Next steps:
#   1. Review each tool — remove any tools your agent should NOT call.
#   2. Add pattern constraints for string parameters (see TODO comments).
#   3. Validate: <path-to-binary> check --policy policy.yaml <audit.log>
#   4. Enforce:  <path-to-binary> start --policy policy.yaml ...
#
# (e.g. .\agentwall.exe check ...)

version: "1"
default_action: deny

session:
  max_calls_per_second: 10

tools:
  - name: "exec_shell"
    action: allow
    parameters:
      - name: "command"
        type: string
        required: true
        # TODO: add pattern constraint for this parameter
        # pattern: ".*"  # Example: restrict to safe values

  - name: "list_directory"
    action: allow
    parameters:
      - name: "directory"
        type: string
        required: true
        # TODO: add pattern constraint for this parameter
        # pattern: ".*"  # Example: restrict to safe values

  - name: "read_file"
    action: allow
    parameters:
      - name: "path"
        type: string
        required: true
        # TODO: add pattern constraint for this parameter
        # pattern: ".*"  # Example: restrict to safe values

  - name: "write_file"
    action: allow
    parameters:
      - name: "content"
        type: string
        required: true
        # TODO: add pattern constraint for this parameter
        # pattern: ".*"  # Example: restrict to safe values
      - name: "path"
        type: string
        required: true
        # TODO: add pattern constraint for this parameter
        # pattern: ".*"  # Example: restrict to safe values

