# Auto-generated by AgentWall from 95 observed tool calls
# Observation window: 2026-06-14 to 2026-06-22
# Tools observed: 3
# Review this policy carefully before enabling enforcement.
# Run: agentwall lint agentwall-policy.yaml

version: "2"
default_action: deny

self_healing:
  enabled: true
  decay_window: 30d
  auto_suggest: true
  suggest_threshold: 0.9
  approval_required: true

tools:
  - name: exec_shell
    action: allow
    # risk_tier: TIER_1  confidence: low  (2 observations)
    # confidence_decay: 0.94  last_seen: 2026-06-20T16:43:23+00:00  stale: false
    parameters:
      - name: command
        type: string
        required: true
        max_length: 64
        # enum:
        #  - "echo Hello World"
        #  - "whoami"

  - name: read_file
    action: allow
    # risk_tier: TIER_3  confidence: medium  (38 observations)
    # confidence_decay: 1.00  last_seen: 2026-06-22T12:18:26+00:00  stale: false
    parameters:
      - name: path
        type: string
        required: true
        max_length: 64
        validators:
          - path_traversal
        # enum:
        #  - "sensitive.txt"
        #  - "obfuscated.txt"
        #  - "obfuscated2.txt"
        #  - "fresh_test.txt"
        #  - "newtest.txt"
        #  - "C:/workspace/test.txt"
        #  - "C:/workspace/source.txt"

  - name: test_tool
    action: allow
    # risk_tier: TIER_3  confidence: medium  (12 observations)
    # confidence_decay: 0.96  last_seen: 2026-06-21T07:56:31+00:00  stale: false
    parameters:
      - name: key
        type: string
        required: true
        max_length: 64
        # enum:
        #  - "value"
        # observed_pattern: "^value.*$"  # informational only

# ── Anomalies (review required) ────────────────────────────────────────
# - read_file.path: observed anomalous value "sensitive.txt" (anomaly_score: 0.95)
#   → Is this expected? Review before enabling enforcement.
