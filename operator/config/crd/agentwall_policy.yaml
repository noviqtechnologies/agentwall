apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: agentwallpolicies.agentwall.io
spec:
  group: agentwall.io
  names:
    kind: AgentWallPolicy
    listKind: AgentWallPolicyList
    plural: agentwallpolicies
    singular: agentwallpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentWallPolicy is the Schema for the agentwallpolicies API
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: AgentWallPolicySpec defines the desired state of AgentWallPolicy
            properties:
              gatewayImage:
                type: string
              policy:
                type: string
              identity:
                properties:
                  provider:
                    type: string
                  vaultAddr:
                    type: string
                type: object
              networkPolicy:
                properties:
                  enforced:
                    type: boolean
                type: object
            type: object
          status:
            description: AgentWallPolicyStatus defines the observed state of AgentWallPolicy
            properties:
              gatewayPodName:
                type: string
              lastReconcileTime:
                type: string
              phase:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
